/**
 * HyperEngine gameplay options.
 */
interface HyperEngineOptions {
  /**
   * Hyper Engine is ready for use?
   */
  isHyperEngineReady: boolean;
  /**
   * Hyper Engine is enbaled?
   */
  hyperEngineEnabled: boolean;
  /**
   * Hyper Engine boost velocity.
   */
  hyperEngineVelocity: number;
  /**
   * Hyper Engine recharge time.
   */
  hyperEngineRechargeTime: number;
  /**
   * Hyper Engine Charges.
   */
  hyperEngineCharges: number;
  /**
   * Hyper Engine use duration time.
   */
  hyperEngineDurationTime: number;
}
